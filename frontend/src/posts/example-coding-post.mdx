Some intro text here. Write prose just like any other post.

## Section Heading

Explanation of the concept.

```typescript
const add = (a: number, b: number) => a + b;

console.log(add(1, 2)); // 3
```

More prose after the code block. You can use **bold**, *italic*, and all standard markdown.

```bash
npm install some-package
```

Keep writing normally between code blocks.
