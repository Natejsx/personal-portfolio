import { PostLayout } from '../components/PostLayout';

<PostLayout
  title="Your Post Title"
  author="Nate"
  date="March 5th, 2026"
  readingTime="5 min read"
  featuredImage="/assets/images/your-image.jpg"
>

Your intro paragraph goes here. Write normally just like any other blog post.

## Section Heading

More prose content here. You can use **bold**, *italic*, and all standard markdown.

### Code Example

Here's how you embed a code block with syntax highlighting:

```typescript
const greet = (name: string): string => {
  return `Hello, ${name}!`;
};

console.log(greet('World'));
```

You can use any language identifier — `javascript`, `typescript`, `python`, `bash`, `css`, etc.

```javascript
// JavaScript example
function add(a, b) {
  return a + b;
}
```

```bash
npm install some-package
```

Regular prose continues after code blocks just fine.

</PostLayout>
